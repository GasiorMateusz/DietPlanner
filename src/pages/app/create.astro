---
import PrivateLayout from "../../layouts/PrivateLayout.astro";
import { AIChatInterfaceWrapper } from "../../components/AIChatInterfaceWrapper";

// Read startup data from sessionStorage (set by Dashboard after form submission)
// and initiate the AI session. The actual session creation happens client-side
// in the AIChatInterface component on mount.
---

<PrivateLayout title="Create Meal Plan - Diet Planner">
  <AIChatInterfaceWrapper client:load />
</PrivateLayout>
